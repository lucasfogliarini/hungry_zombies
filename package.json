{
  "name": "hungry_zombies",
  "private": true,
  "scripts": {
    "start": "meteor run",
      "test": "SERVER_TEST_REPORTER='dot' meteor test --once --driver-package dispatch:mocha"
  },
  "dependencies": {
    "babel-runtime": "6.18.0",
    "bcrypt": "^1.0.2",
    "meteor-node-stubs": "~0.2.0"
  }
}
